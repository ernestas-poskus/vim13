#!/bin/sh

if [ ! -d "$HOME/vim13" ]; then
  echo "Installing vim13"
  git clone https://github.com/ernestas-poskus/vim13.git $HOME/dot13
fi

mkdir -p $HOME/.vim/autoload > /dev/null 2>&1
mkdir -p $HOME/.vim/colors > /dev/null 2>&1
mkdir -p $HOME/.vim/undodir > /dev/null 2>&1
mkdir -p $HOME/.vim/colors > /dev/null 2>&1

# Vim plugin manager
curl -fLo $HOME/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim

# Color scheme
curl -fLo $HOME/.vim/colors/molokai.vim --create-dirs \
    https://raw.githubusercontent.com/tomasr/molokai/master/colors/molokai.vim

ln -sf $HOME/vim13/languages $HOME/.vim/indent
ln -sf $HOME/vim13/vimrc $HOME/.vimrc

# Support NEOVIM
ln -sf $HOME/vim13/vimrc $HOME/.nvimrc
ln -sf $HOME/.vim $HOME/.nvim
